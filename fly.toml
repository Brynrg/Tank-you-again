# fly.toml app configuration file generated for tank-you-again on 2026-05-16T18:19:54-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'tank-you-again'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  HOST = '0.0.0.0'
  PORT = '3001'

[http_service]
  internal_port = 3001
  force_https = true
  # Single global in-memory arena: the room lives in one process, so the app
  # runs as exactly ONE always-warm machine (see `flyctl scale count 1`).
  # Keeping it warm also removes the cold-start delay on first connect.
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
